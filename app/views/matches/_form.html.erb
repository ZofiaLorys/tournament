  <%= form_with(scope: :matches, url: "/matches", method: "post") do |f| %>
    <%= f.hidden_field(:group_name, :value => group_name) %>
    <%= f.submit("Create tournament pairs for a group #{group_name}") %>
  <% end %>