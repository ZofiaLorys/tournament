  <%= form_with(scope: :teams, url: "/teams", method: "post") do |f| %>
    <%= f.hidden_field(:group_name, :value => group_name) %>
    <%= f.submit("Create 8 teams in a group #{group_name}") %>
  <% end %>